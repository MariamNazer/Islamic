import 'package:flutter/material.dart';

class SettingeTab extends StatelessWidget {
  const SettingeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}